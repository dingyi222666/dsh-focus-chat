nodeLinker: node-modules
npmRegistryServer: "https://registry.npmjs.org"
npmScopes:
  deepseek-ai:
    npmRegistryServer: "https://registry.npmjs.org"
    # rc.7 刚发布，yarn 的发布年龄隔离（npmMinimalAgeGate）会拦截新版本；
    # 该 scope 是我们固定依赖的发布方，按 scope 关闭隔离。
    npmMinimalAgeGate: 0
